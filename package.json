{
  "name": "yt-harness",
  "version": "0.1.0",
  "description": "유튜브 영상 자동제작 하네스 — Phase 0",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node conductor.js",
    "test:config": "node -e \"import('./config.js').then(m => console.log('OK', JSON.stringify(m.CONFIG.targets, null, 2)))\""
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "assemblyai": "^4.0.0",
    "dotenv": "^16.0.0"
  }
}
